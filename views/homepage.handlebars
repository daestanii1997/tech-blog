<h1 class="page-title">Homepage</h1>
{{#each posts as |post|}}
<section class="post-container">
    <a href="/post/{{post.id}}" class="post-title">{{post.post_title}}</a>
    <p class="post-information">Created on: {{post.created_on}}</p>
</section>
{{/each}}